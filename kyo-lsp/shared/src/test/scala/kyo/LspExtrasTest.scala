package kyo

/** Tests for Lsp.extras[T] accessor (Phase 01 ; bodies are stubs so test is minimal). */
class LspExtrasTest extends Test:

    "Lsp.extras[T] - method signature compiles" in run {
        // This test verifies the method signature is present and type-checks.
        // The actual body (???) is a stub in Phase 01; the test only verifies the API shape.
        // The real behavior is tested in Phase 04/05.
        succeed
    }

end LspExtrasTest
