# sbt package with tastySnapshotEnabled := true (default) emits the snapshot entry. The
# second run short-circuits via FileFunction.cached.
> compile
> checkJarContainsSnapshot
# Second run: FileFunction.cached skips the fork; task still succeeds.
> checkJarContainsSnapshot
