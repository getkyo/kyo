# Verifies that the bundled runner is extracted on first invocation and reused
# (idempotent) on the second.
> compile
> tastySnapshot
> checkRunnerExtracted
> checkSnapshot
# Second run: extraction is idempotent (no error on re-run).
> tastySnapshot
> checkRunnerExtracted
> checkSnapshot
