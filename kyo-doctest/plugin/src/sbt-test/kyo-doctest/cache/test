# Cache: first run compiles, second run reports cache hits (cacheHits=1).
# The summary sits in doctestCacheDir, which is the build root's .doctest-cache.
> doctest
> doctest
$ exists .doctest-cache/last-summary.txt
$ exec grep -q cacheHits=1 .doctest-cache/last-summary.txt
