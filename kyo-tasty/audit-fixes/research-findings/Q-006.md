# Research finding Q-006

Question: After the routine Symbol and Classpath accessors take (using AllowUnsafe), does every existing callsite in kyo-tasty, kyo-ts, and kyo-flow either have AllowUnsafe in scope or sit inside Sync.Unsafe.defer?
Type: research-knowable
Budget: 10 minutes (used: 8)

## Answer

Total callsite count across kyo-tasty (including tests) is 159 sites (153 accessors + 6 Symbol.position calls). kyo-flow has zero callsites (no kyo-tasty imports). kyo-ts uses `tastyquery.Symbols.*` exclusively, not `kyo.Tasty.Symbol`, so its 40+ accessor calls are entirely out of scope.

ALREADY-PROOF: 133 sites. Every internal decoder module already imports AllowUnsafe locally: `Classpath.scala` (12 sites, each pure accessor has its own `import AllowUnsafe.embrace.danger`), `Tasty.scala` internal methods (16 sites, inline imports at each accessor body and assignHomes), `ClasspathOrchestrator.scala` (3 sites, local imports at 375, 389, 446), `SnapshotWriter.scala` (2 sites, file-level import at line 61), `Subtyping.scala` (4 sites, imports at 139 and 184), `Scala2PickleReader.scala` (6 sites, imports at 284, 369, 401, 445), `ClassfileUnpickler.scala` (2 sites, import at 73), `ClasspathTestHelpers.scala` (2 sites, import at 18). Test callsites in `QueryApiTest`, `CommentsUnpicklerTest`, `SymbolResolutionTest` have explicit local imports. All remaining test callsites (`JavaSymbolTest`, `ClassfileReaderTest`, `PositionsUnpicklerTest`, `SnapshotRoundTripTest`, `SnapshotRoundTripJvmTest`, `AstUnpicklerTest`, `TreeUnpicklerTest`, `Scala2PickleTest`, `ClasspathRefDedupTest`) are inside `in run { ... }` blocks that route through `BaseKyoCoreTest.run`, which contains `import AllowUnsafe.embrace.danger` and `Sync.Unsafe.evalOrThrow` at lines 8 and 18.

SYNC-WRAPPABLE: 26 sites. The four example files (`IdeHoverExample`, `RuntimeReflectionExample`, `CodegenExample`, `JavaScalaBridgeExample`) have methods returning `< (Sync & Async & Abort[TastyError] & Scope)` and can take `(using AllowUnsafe)` propagated from callers. `ClasspathOrchestratorPipelineTest` wraps its two `allSymbols.fullName` chains in `Sync.defer` at lines 84 and 194-195. `SnapshotRoundTripJvmTest:148` wraps `allSymbols.toSeq.find` in `Sync.defer`. The public `Classpath` extension methods in `Tasty.scala` (`findClass`, `findPackage`, `topLevelClasses`, `packages`, `findModule`, `findClassByBinary` at lines 1014-1057) delegate to internal `pureClass`/`purePackage` etc.; these six wrappers become SYNC-WRAPPABLE by taking `(using AllowUnsafe)` themselves, which their all-test callers already provide via the test framework. `TypeOps.applied`/`andType` (3 sites) and `ClasspathOrchestrator.nameToString` (1 site) can propagate `(using AllowUnsafe)` from the enclosing `Sync`-effect decode chain.

REFACTOR-NEEDED: 0 sites. No callsite exists that is unreachable by proof propagation. Every site either already has an `import AllowUnsafe.embrace.danger` in scope or resides inside a `Sync`-effect context where `(using AllowUnsafe)` can be threaded through from the entry boundary.

Every callsite is categorized as ALREADY-PROOF or SYNC-WRAPPABLE; refactor risk is zero (no site requires a design escalation).

## Citations

1. /Users/fwbrasil/workspace/kyo/.claude/worktrees/cached-inventing-quasar/kyo-tasty/shared/src/main/scala/kyo/internal/tasty/query/Classpath.scala:68-70
   `private[kyo] def pureClass(fqn: String): Maybe[Tasty.Symbol] = ... import AllowUnsafe.embrace.danger`

2. /Users/fwbrasil/workspace/kyo/.claude/worktrees/cached-inventing-quasar/kyo-tasty/shared/src/test/scala/kyo/ClasspathOrchestratorPipelineTest.scala:84
   `Sync.defer(rawCp.allSymbols.map(_.fullName.asString).toSet)` — already wrapped in Sync.defer

3. /Users/fwbrasil/workspace/kyo/.claude/worktrees/cached-inventing-quasar/kyo-core/shared/src/main/scala/kyo/internal/BaseKyoCoreTest.scala:8
   `import AllowUnsafe.embrace.danger` inside `def run(...)` — proves all `in run { ... }` test blocks are ALREADY-PROOF

4. /Users/fwbrasil/workspace/kyo/.claude/worktrees/cached-inventing-quasar/kyo-tasty/shared/src/main/scala/kyo/Tasty.scala:994-996
   `import AllowUnsafe.embrace.danger; val syms = cp.allSymbols` inside assignHomes

5. /Users/fwbrasil/workspace/kyo/.claude/worktrees/quirky-pondering-wadler/kyo-ts/jvm/src/main/scala/kyo/gen/Main.scala:1-5
   `import tastyquery.Symbols.*` (not kyo.Tasty.Symbol) — confirms kyo-ts is out of scope

## Confidence

high

Every callsite was read in context; the BaseKyoCoreTest.run finding covers the bulk of test sites mechanically; internal decoder sites all have existing local imports; the kyo-ts out-of-scope verdict is proven by import inspection.

## Deferral-pattern self-check

`flow-verify-grep.sh --catalog open-question --target research-findings/Q-006.md`: 0 hits.
