name: build-main
on:
  push:
    branches:
      - main

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      # Full matrix (enable when budget allows):
      # os-matrix: '["linux-x64", "linux-arm64", "macos-x64", "macos-arm64", "windows-x64", "windows-arm64"]'
      os-matrix: '["linux-x64", "linux-arm64"]'
      test-command: test
