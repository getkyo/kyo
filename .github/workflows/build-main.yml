name: build-main
on:
  push:
    branches:
      - main

# One job per arch. They run in parallel; each internally serializes JVM → JS → Native
# via build.yml's `max-parallel: 1` matrix. Add or comment out arches here when budget
# allows; the runner-image mapping is duplicated below to keep the workflow
# self-contained.

jobs:
  linux-x64:
    uses: ./.github/workflows/build.yml
    with:
      os: linux-x64
      runner-image: ubuntu-latest
      test-command: test

  linux-arm64:
    uses: ./.github/workflows/build.yml
    with:
      os: linux-arm64
      runner-image: ubuntu-24.04-arm
      test-command: test

  # Uncomment to extend the matrix when budget allows:
  # macos-x64:
  #   uses: ./.github/workflows/build.yml
  #   with:
  #     os: macos-x64
  #     runner-image: macos-latest-large
  #     test-command: test
  #
  # macos-arm64:
  #   uses: ./.github/workflows/build.yml
  #   with:
  #     os: macos-arm64
  #     runner-image: macos-latest-xlarge
  #     test-command: test
  #
  # windows-x64:
  #   uses: ./.github/workflows/build.yml
  #   with:
  #     os: windows-x64
  #     runner-image: build-windows-x64
  #     test-command: test
  #
  # windows-arm64:
  #   uses: ./.github/workflows/build.yml
  #   with:
  #     os: windows-arm64
  #     runner-image: build-windows-arm
  #     test-command: test
