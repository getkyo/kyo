name: actionlint
on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/**'

jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7.0.0
      - uses: rhysd/actionlint@v1.7.12
