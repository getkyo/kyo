export declare class Text {
  charAt(index: number): Char;
  compact(): Text;
  compareToIgnoreCase(other: Text): number;
  contains(substr: Text): boolean;
  count(p: Predicate): number;
  drop(n: number): Text;
  dropRight(n: number): Text;
  dropUntilNext(p: Predicate): Text;
  dropWhile(p: Predicate): Text;
  endsWith(suffix: Text): boolean;
  exists(p: Predicate): boolean;
  filter(p: Predicate): Text;
  filterNot(p: Predicate): Text;
  forall(p: Predicate): boolean;
  head(): Maybe<Char>;
  indexOf(substr: Text): number;
  indexWhere(p: Predicate): number;
  is(other: Text): boolean;
  isEmpty(): boolean;
  lastIndexOf(substr: Text): number;
  lastIndexWhere(p: Predicate): number;
  length(): number;
  plus(other: Text): Text;
  reverse(): Text;
  show(): string;
  size(): number;
  span(p: Predicate): [Text, Text];
  split(separator: Char): Chunk<Text>;
  startsWith(prefix: Text): boolean;
  stripPrefix(prefix: Text): Text;
  stripSuffix(suffix: Text): Text;
  substring(from: number, until: number): Text;
  tail(): Text;
  take(n: number): Text;
  takeRight(n: number): Text;
  takeUntilNext(p: Predicate): Text;
  takeWhile(p: Predicate): Text;
  toChunk(): Chunk<Char>;
  trim(): Text;

  readonly empty: Text;
};