export declare class Span {
  append(x: A): ReadonlyArray<A>;
  apply(idx: number): A;
  collect<B>(pf: PartialFunction<A, B>): ReadonlyArray<B>;
  collectFirst<B>(pf: PartialFunction<A, B>): Maybe<B>;
  concat(suffix: ReadonlyArray<A>): ReadonlyArray<A>;
  contains(elem: A): boolean;
  copyToArray<B>(xs: B[], start: number, len: number): number;
  count(p: (x1: A) => boolean): number;
  distinct(): ReadonlyArray<A>;
  distinctBy<B>(f: (x1: A) => B): ReadonlyArray<A>;
  drop(n: number): ReadonlyArray<A>;
  dropRight(n: number): ReadonlyArray<A>;
  dropWhile(p: (x1: A) => boolean): ReadonlyArray<A>;
  endsWith<B>(that: ReadonlyArray<B>): boolean;
  exists(f: (x1: A) => boolean): boolean;
  existsZip<B>(b: ReadonlyArray<B>, f: (x1: A, x2: B) => boolean): boolean;
  existsZip<B, C>(b: ReadonlyArray<B>, c: ReadonlyArray<C>, f: (x1: A, x2: B, x3: C) => boolean): boolean;
  filter(p: (x1: A) => boolean): ReadonlyArray<A>;
  filterNot(p: (x1: A) => boolean): ReadonlyArray<A>;
  find(p: (x1: A) => boolean): Maybe<A>;
  flatMap<B>(f: (x1: A) => ReadonlyArray<B>): ReadonlyArray<B>;
  flatten<B>(): ReadonlyArray<B>;
  fold<B>(z: B, op: (x1: B, x2: B) => B): B;
  foldLeft<B>(z: B, op: (x1: B, x2: A) => B): B;
  foldRight<B>(z: B, op: (x1: A, x2: B) => B): B;
  forall(f: (x1: A) => boolean): boolean;
  forallZip<B>(b: ReadonlyArray<B>, f: (x1: A, x2: B) => boolean): boolean;
  forallZip<B, C>(b: ReadonlyArray<B>, c: ReadonlyArray<C>, f: (x1: A, x2: B, x3: C) => boolean): boolean;
  foreach(f: (x1: A) => unknown): void;
  head(): Maybe<A>;
  indexOf(elem: A, from: number): Maybe<number>;
  indexWhere(p: (x1: A) => boolean, from: number): Maybe<number>;
  is(other: ReadonlyArray<A>): boolean;
  isEmpty(): boolean;
  last(): Maybe<A>;
  lastIndexOf(elem: A, end: number): Maybe<number>;
  lastIndexWhere(p: (x1: A) => boolean, end: number): Maybe<number>;
  map<B>(f: (x1: A) => B): ReadonlyArray<B>;
  mkString(separator: string): string;
  mkString(): string;
  mkString(start: string, sep: string, end: string): string;
  nonEmpty(): boolean;
  padTo(len: number, elem: A): ReadonlyArray<A>;
  partition(p: (x1: A) => boolean): [ReadonlyArray<A>, ReadonlyArray<A>];
  prepend(x: A): ReadonlyArray<A>;
  reverse(): ReadonlyArray<A>;
  scan(z: A, op: (x1: A, x2: A) => A): ReadonlyArray<A>;
  scanLeft<B>(z: B, op: (x1: B, x2: A) => B): ReadonlyArray<B>;
  scanRight<B>(z: B, op: (x1: A, x2: B) => B): ReadonlyArray<B>;
  size(): number;
  slice(from: number, until: number): ReadonlyArray<A>;
  sliding(size: number, step: number): Iterator<ReadonlyArray<A>>;
  span(p: (x1: A) => boolean): [ReadonlyArray<A>, ReadonlyArray<A>];
  splitAt(n: number): [ReadonlyArray<A>, ReadonlyArray<A>];
  startsWith<B>(that: ReadonlyArray<B>, offset: number): boolean;
  tail(): Maybe<ReadonlyArray<A>>;
  take(n: number): ReadonlyArray<A>;
  takeRight(n: number): ReadonlyArray<A>;
  takeWhile(p: (x1: A) => boolean): ReadonlyArray<A>;
  toArray(): A[];
  toArrayUnsafe(): A[];
  update(index: number, elem: A): ReadonlyArray<A>;

  static apply<A>(a0: A, a1: A, a2: A): ReadonlyArray<A>;
  static apply<A>(a0: A): ReadonlyArray<A>;
  static apply<A>(): ReadonlyArray<A>;
  static apply<A>(a0: A, a1: A): ReadonlyArray<A>;
  static apply<A>(a0: A, a1: A, a2: A, a3: A, a4: A): ReadonlyArray<A>;
  static apply<A>(a0: A, a1: A, a2: A, a3: A): ReadonlyArray<A>;
  static concat<A>(spans: ReadonlyArray<A>[]): ReadonlyArray<A>;
  static empty<A>(): ReadonlyArray<A>;
  static from<A>(seq: IterableOnce<A>): ReadonlyArray<A>;
  static fromUnsafe<A>(array: A[]): ReadonlyArray<A>;
  static iterate<A>(start: A, len: number, f: (x1: A) => A): ReadonlyArray<A>;
};